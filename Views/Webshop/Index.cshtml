@{
    ViewData["Title"] = "Home Page";
}


@model List<Product>

<div class="container">
    @foreach (var element in Model)
    {
        @await Component.InvokeAsync("ProductCard", element)
    }
</div>